name: lancast_arena
description: LanCast Arena — cyber esports HUD theme and battle UI primitives.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
