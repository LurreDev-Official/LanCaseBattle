name: lancast_core
description: Shared LanCast domain models, enums, and protocol envelope.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.6
