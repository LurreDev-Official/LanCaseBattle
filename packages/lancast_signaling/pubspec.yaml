name: lancast_signaling
description: LanCast WebSocket signaling host and client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.1

resolution: workspace

dependencies:
  lancast_core: ^0.1.0
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
