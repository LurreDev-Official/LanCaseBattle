name: lancast_webrtc
description: WebRTC helpers for LanCast screen share (LAN).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.14.1
  lancast_core: ^0.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
